import { StoreInitializer } from "@/app/store/initializer";

export default function PageApp() {

    return (
        <>
            {/* <StoreInitializer
                thread={"ikfkw"}
                list={[]}
                session={{
                    authenticated : true,
                    username : "Josh"
                }}
                loaded_thread={null}
                contacts={[]}
            /> */}
        </>
    );
}